# @lxdll/prettier-config
prettier config.

# Install
```shell
npm i -D @lxdll/prettier-config
```

# Features
- No semicolons.
- Single quotes.
- Trailing commas.
- Ignore common files.(eg: `node_modules`, `dist` ...)
